import { expect } from "chai";
import { ethers } from "hardhat";

describe("withdraw", function () {
    it("Should withdraw funds if eligible for dividend", async function () {

        expect(await greeter.greet()).to.equal("Hola, mundo!");
    });
  });
  